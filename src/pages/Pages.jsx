import { useMemo, useState } from 'react'
import { ArrowRight, Check, CircleHelp, DollarSign, Gauge, Lightbulb, Play, SlidersHorizontal, Sparkles, Target, Wallet } from 'lucide-react'
import { organizationSummary } from '../data/mockData'
import { expectedLoss, riskLevel, riskScore, simulateInvestment, totalExposure } from '../utils/riskCalculations'
import { useAppState } from '../state/AppState'

const money = (lakhs) => lakhs >= 100 ? `₹${(lakhs / 100).toFixed(2)} Cr` : `₹${lakhs.toFixed(lakhs % 1 ? 2 : 0)}L`
const Badge = ({ children, tone = '' }) => <span className={`status-tag ${(tone || children).toLowerCase()}`}>{children}</span>
const readStoredName = () => {
  try {
    const identity = JSON.parse(localStorage.getItem('cyberRiskAiIdentity'))
    return identity?.name?.trim() || ''
  } catch {
    return ''
  }
}
const PageTitle = ({ eyebrow, title, copy, action }) => <div className="page-heading"><div><p className="eyebrow">{eyebrow.startsWith('Tuesday') ? 'Security overview' : eyebrow}</p><h1>{title.startsWith('Good morning,') ? `Good morning${readStoredName() ? `, ${readStoredName()}` : ''}.` : title}</h1><p className="subheading">{copy}</p></div>{action}</div>
const Metric = ({ label, value, detail, tone = '' }) => <article className="metric-card"><div className="metric-top"><span>{label}</span><Gauge size={17} className={`metric-icon ${tone}`} /></div><strong className="metric-number">{value}</strong><div className="metric-bottom"><span className="trend positive">{detail}</span></div></article>

export function Dashboard({ navigate }) {
  const { assets, budget, optimizedResult, appliedControls } = useAppState()
  const exposure = totalExposure(assets)
  const simulated = simulateInvestment(appliedControls, organizationSummary.baselineRisk, exposure)
  return <><PageTitle eyebrow="Tuesday, September 2, 2026  •  Security overview" title="Good morning, User Name." copy="Your organization-wide cyber risk and investment overview." action={<button className="primary-button" type="button" onClick={() => navigate('optimizer')}><Target size={16} /> Optimize budget</button>} /><section className="metric-grid"><article className="metric-card featured"><div className="metric-top"><span>Overall cyber risk</span><span className="trend positive">{simulated.riskReduction ? `↓ ${simulated.riskReduction} pts` : 'Stable'}</span></div><div className="score-row"><strong>{simulated.projectedRisk}</strong><span>/100</span><div className="score-ring"><div></div></div></div><div className="metric-bottom"><span><span className="health-dot"></span>{simulated.riskReduction ? 'After investment' : 'High priority'}</span><span>current projection</span></div></article><Metric label="Expected financial loss" value={money(simulated.projectedLoss)} detail={`${money(exposure)} before controls`} tone="red" /><Metric label="Critical vulnerabilities" value={String(organizationSummary.criticalVulnerabilities).padStart(2, '0')} detail="3 need action" tone="red" /><Metric label="Vulnerable assets" value={organizationSummary.vulnerableAssets} detail="+2 this month" tone="blue" /><Metric label="Available budget" value={money(budget)} detail={`${money(optimizedResult.remaining)} unallocated`} tone="green" /></section><div className="content-grid"><section className="panel"><div className="panel-header"><div><h2>Risk by asset</h2><p>Exposure weighted by impact and threat probability.</p></div><button className="text-button" type="button" onClick={() => navigate('assessment')}>View assessment <ArrowRight size={14} /></button></div><div className="chart-area">{assets.map((asset) => <div className="chart-row" key={asset.id}><span>{asset.name}</span><div className="chart-track"><i className={asset.criticality.toLowerCase()} style={{ width: `${Math.min(riskScore(asset), 100)}%` }}></i></div><strong>{riskScore(asset)}</strong></div>)}</div></section><section className="panel"><div className="panel-header"><div><h2>Financial exposure</h2><p>Expected loss by asset, in lakhs.</p></div><DollarSign size={17} className="metric-icon" /></div><div className="financial-chart"><div className="donut"><strong>{money(exposure)}</strong><span>Total exposure</span></div><div className="legend">{assets.map((asset) => <div key={asset.id}><i className={asset.criticality.toLowerCase()}></i><span>{asset.name}</span><strong>{money(expectedLoss(asset))}</strong></div>)}</div></div></section></div><section className="insight-banner"><div className="insight-icon"><Sparkles size={19} /></div><div><span className="eyebrow">Recommended action</span><h2>₹19L can reduce projected exposure by ₹70L</h2><p>Prioritize the top five controls to target the highest-value risk paths first.</p></div><button className="secondary-button" type="button" onClick={() => navigate('recommendations')}>Review actions <ArrowRight size={14} /></button></section></>
}

export function RiskAssessment() {
  const { assets, selectedAsset: selected, setSelectedAssetId } = useAppState()
  const [filter, setFilter] = useState('All')
  const [sortDescending, setSortDescending] = useState(true)
  const visibleAssets = useMemo(() => assets.filter((asset) => filter === 'All' || asset.criticality === filter).sort((a, b) => sortDescending ? riskScore(b) - riskScore(a) : riskScore(a) - riskScore(b)), [assets, filter, sortDescending])
  return <><PageTitle eyebrow="01 / Understand" title="Risk assessment" copy="Inspect the factors behind each asset's explainable risk score." /><div className="panel assessment-panel"><div className="table-toolbar"><div><h2>Vulnerability register</h2><p>Click an asset to inspect its risk factors.</p></div><div className="toolbar-actions"><select value={filter} onChange={(event) => setFilter(event.target.value)} aria-label="Filter by criticality"><option>All</option><option>Critical</option><option>High</option><option>Medium</option></select><button className="filter-button" type="button" onClick={() => setSortDescending(!sortDescending)}><SlidersHorizontal size={14} /> Sort {sortDescending ? 'high to low' : 'low to high'}</button></div></div><div className="table-wrap"><table><thead><tr><th>Asset / vulnerability</th><th>CVSS</th><th>Probability</th><th>Criticality</th><th>Risk score</th><th>Financial impact</th><th>Level</th></tr></thead><tbody>{visibleAssets.map((asset) => <tr className={selected.id === asset.id ? 'row-selected' : ''} key={asset.id} onClick={() => setSelectedAssetId(asset.id)}><td><strong>{asset.name}</strong><span>{asset.vulnerability}</span></td><td>{asset.cvss}</td><td>{Math.round(asset.probability * 100)}%</td><td><Badge>{asset.criticality}</Badge></td><td><strong>{riskScore(asset)}</strong>/100</td><td>{money(asset.impact)}</td><td><Badge tone={riskLevel(riskScore(asset))}>{riskLevel(riskScore(asset))}</Badge></td></tr>)}</tbody></table></div></div><div className="detail-grid"><section className="panel detail-card"><div className="panel-header"><div><p className="eyebrow">Selected asset</p><h2>{selected.name}</h2><p>{selected.exposure} · Owned by {selected.owner}</p></div><Badge tone={riskLevel(riskScore(selected))}>{riskLevel(riskScore(selected))}</Badge></div><div className="factor-list"><div><span>Vulnerability</span><strong>{selected.vulnerability}</strong></div><div><span>CVSS severity</span><strong>{selected.cvss} / 10</strong></div><div><span>Threat probability</span><strong>{Math.round(selected.probability * 100)}%</strong></div><div><span>Expected loss</span><strong>{money(expectedLoss(selected))}</strong></div><div><span>Recommended action</span><strong>{selected.recommendation}</strong></div></div></section><section className="panel explanation"><div className="panel-header"><div><h2>How this score works</h2><p>Transparent prototype model</p></div><CircleHelp size={17} className="metric-icon" /></div><p>Risk score combines vulnerability severity, threat probability, asset criticality, and business impact. This is an explainable prototype model, not a scientifically validated enterprise standard.</p><div className="formula">CVSS × probability × criticality weight</div></section></div></>
}

export function FinancialRisk() {
  const { assets, selectedAsset: selected, selectedAssetId, setSelectedAssetId } = useAppState()
  return <><PageTitle eyebrow="02 / Quantify" title="Financial risk" copy="Translate cyber events into a language the business can act on." /><div className="selector-row"><label>Analyze asset<select value={selectedAssetId} onChange={(event) => setSelectedAssetId(event.target.value)}>{assets.map((asset) => <option key={asset.id} value={asset.id}>{asset.name} · {asset.vulnerability}</option>)}</select></label></div><div className="financial-detail-grid"><section className="panel calculation-card"><div className="panel-header"><div><h2>{selected.name}</h2><p>{selected.vulnerability} · {selected.criticality} asset</p></div><Badge tone={riskLevel(riskScore(selected))}>{riskLevel(riskScore(selected))}</Badge></div><div className="loss-equation"><div><span>Threat probability</span><strong>{Math.round(selected.probability * 100)}%</strong></div><b>×</b><div><span>Potential impact</span><strong>{money(selected.impact)}</strong></div><b>=</b><div className="loss-result"><span>Expected loss</span><strong>{money(expectedLoss(selected))}</strong></div></div><div className="calculation-note">Expected Loss = Probability of Incident × Potential Financial Impact</div></section><section className="panel"><div className="panel-header"><div><h2>Exposure across assets</h2><p>Total expected loss: {money(totalExposure(assets))}</p></div><Wallet size={17} className="metric-icon green" /></div><div className="exposure-bars">{assets.map((asset) => <div className="exposure-bar" key={asset.id}><div><span>{asset.name}</span><strong>{money(expectedLoss(asset))}</strong></div><div className="chart-track"><i className={asset.criticality.toLowerCase()} style={{ width: `${expectedLoss(asset) / 10}%` }}></i></div></div>)}</div></section></div></>
}

export function Recommendations({ navigate }) {
  const { recommendations, markedRecommendationIds, toggleRecommendation } = useAppState()
  return <><PageTitle eyebrow="03 / Decide" title="Recommended actions" copy="Deterministic recommendations ranked by risk reduction value." action={<button className="primary-button" type="button" onClick={() => navigate('optimizer')}><Target size={16} /> Open optimizer</button>} /><div className="recommendation-toolbar"><span>{markedRecommendationIds.length} action{markedRecommendationIds.length === 1 ? '' : 's'} marked for investment</span><button className="text-button" type="button" onClick={() => markedRecommendationIds.forEach(toggleRecommendation)}>Clear selection</button></div><div className="recommendation-grid">{recommendations.map((item, index) => { const selected = markedRecommendationIds.includes(item.id); return <article className={`panel recommendation-card ${selected ? 'recommendation-selected' : ''}`} key={item.id}><div className="recommendation-top"><span className="rec-index">0{index + 1}</span><Badge tone={item.priority}>{item.priority}</Badge></div><h2>{item.action}</h2><p className="related">Problem · {item.related} · {item.vulnerability}</p><div className="recommendation-stats"><div><span>Investment</span><strong>{money(item.cost)}</strong></div><div><span>Risk reduction</span><strong className="positive-text">{money(item.reduction)}</strong></div><div><span>Financial benefit</span><strong className="positive-text">{money(item.reduction)}</strong></div></div><p className="reason"><Lightbulb size={15} />{item.reason}</p><button className={`select-action ${selected ? 'selected' : ''}`} type="button" onClick={() => toggleRecommendation(item.id)}>{selected ? <><Check size={14} /> Marked for investment</> : 'Mark for investment'}</button></article> })}</div></>
}

export function InvestmentOptimizer() {
  const { recommendations, budget, setBudget, optimizedResult, applyOptimizedPlan } = useAppState()
  return <><PageTitle eyebrow="04 / Allocate" title="Investment optimizer" copy="Find the highest-impact security allocation within your budget." action={<button className="primary-button" type="button" onClick={applyOptimizedPlan}><Play size={15} /> Apply optimized plan</button>} /><section className="optimizer-controls panel"><label>Available cybersecurity budget<input type="number" min="0" value={budget} onChange={(event) => setBudget(event.target.value)} /><span>₹ Lakhs</span></label><div className="budget-quick">{[5,10,15,20,25,30].map((value) => <button className={budget === value ? 'selected' : ''} type="button" key={value} onClick={() => setBudget(value)}>₹{value}L</button>)}</div><p>Controls are ranked by risk reduction per rupee while respecting your budget.</p></section><div className="optimizer-summary"><Metric label="Total allocated" value={money(optimizedResult.allocated)} detail={`${optimizedResult.selected.length} controls selected`} tone="green" /><Metric label="Remaining budget" value={money(optimizedResult.remaining)} detail="Available to reserve" tone="blue" /><Metric label="Expected reduction" value={money(optimizedResult.reduction)} detail="Projected financial benefit" tone="green" /><Metric label="Projected risk" value={`${organizationSummary.baselineRisk - Math.min(organizationSummary.baselineRisk - 1, Math.round(optimizedResult.reduction / 2))}/100`} detail="From baseline" tone="red" /></div><section className="panel"><div className="table-toolbar"><div><h2>Recommended allocation</h2><p>Ranked by risk-reduction-to-cost value.</p></div></div><div className="control-list">{recommendations.map((item) => { const selected = optimizedResult.selected.some((control) => control.id === item.id); return <div className={`control-row ${selected ? 'selected' : ''}`} key={item.id}><div className={`check-box ${selected ? 'checked' : ''}`}>{selected && <Check size={14} />}</div><div><strong>{item.action}</strong><span>{item.priority} priority · {item.related}</span></div><strong>{money(item.cost)}</strong><span className="reduction">-{money(item.reduction)}</span><span className="value-badge">{(item.reduction / item.cost).toFixed(1)}×</span></div> })}</div></section></>
}

export function Simulation() {
  const { assets, recommendations, appliedControlIds, applyOptimizedPlan, toggleAppliedControl } = useAppState()
  const selectedControls = recommendations.filter((item) => appliedControlIds.includes(item.id))
  const baselineLoss = totalExposure(assets)
  const simulated = simulateInvestment(selectedControls, organizationSummary.baselineRisk, baselineLoss)
  return <><PageTitle eyebrow="05 / Prove impact" title="Risk reduction simulation" copy="Select controls or apply the latest optimized allocation." action={<button className="secondary-button" type="button" onClick={applyOptimizedPlan}><Play size={14} /> Use optimized plan</button>} /><div className="before-after"><section className="panel simulation-card before"><span className="sim-label">Before investment</span><strong>{organizationSummary.baselineRisk}<span>/100</span></strong><p>Expected loss <b>₹82.75L</b></p><div className="sim-meter"><i style={{ width: `${organizationSummary.baselineRisk}%` }}></i></div></section><div className="sim-arrow"><ArrowRight size={24} /></div><section className="panel simulation-card after"><span className="sim-label">After selected controls</span><strong>{simulated.projectedRisk}<span>/100</span></strong><p>Expected loss <b>{money(simulated.projectedLoss)}</b></p><div className="sim-meter"><i style={{ width: `${simulated.projectedRisk}%` }}></i></div></section></div><div className="simulation-summary"><Metric label="Risk reduction" value={`${simulated.riskReduction} points`} detail="Lower residual risk" tone="green" /><Metric label="Financial reduction" value={money(simulated.reduction)} detail="Avoided expected loss" tone="green" /><Metric label="Controls applied" value={selectedControls.length} detail="Selected investments" tone="blue" /></div><section className="panel"><div className="panel-header"><div><h2>Choose investments</h2><p>Toggle controls to see the projected outcome update immediately.</p></div></div><div className="control-list">{recommendations.map((item) => <button className={`control-row simulation-control ${appliedControlIds.includes(item.id) ? 'selected' : ''}`} type="button" key={item.id} onClick={() => toggleAppliedControl(item.id)}><div className={`check-box ${appliedControlIds.includes(item.id) ? 'checked' : ''}`}>{appliedControlIds.includes(item.id) && <Check size={14} />}</div><div><strong>{item.action}</strong><span>{item.priority} priority</span></div><strong>{money(item.cost)}</strong><span className="reduction">-{money(item.reduction)}</span></button>)}</div></section></>
}
